/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '',
    distDir: 'out',
    output: 'export',
    poweredByHeader: false,
    reactStrictMode: true,
    trailingSlash: true,
};

export default nextConfig;
